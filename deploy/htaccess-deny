# Block direct web access to this folder.
# Apache 2.4+ syntax; fall back to legacy if the host's still on 2.2.

<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
